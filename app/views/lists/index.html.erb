<div class= "container m-3">
  <nav class="navbar navbar-expand-lg navbar-light bg-light navbar-lewagon">
    <a class="navbar-brand" href="#">Movie nights..</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse d-flex justify-content-around " id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link active" href="#">Lists <span class="sr-only">(current)</span></a>
        <%= link_to "create a new list", new_list_path, class: "nav-item nav-link btn btn-success" %>
      </div>
    </div>
  </nav>
</div>


<%# <h1>Here are the lists:</h1> %>

    <%# <ul id="lists"> %>
      <%# <% @lists.each do |list| %>

      <%# <%= link_to list.name, list_path(list.id) %>
          <%# <% end %>
    <%# </ul> %>

  <%# <%= link_to "create a new list", new_list_path(@list), class: "btn btn-primary"%>
